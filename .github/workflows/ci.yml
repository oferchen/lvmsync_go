name: CI

on:
  push:
  pull_request:

jobs:
  build:
    runs-on: ubuntu-latest
    container:
      image: almalinux:8
    steps:
      - name: Install dependencies
        run: |
          dnf -y install git golang
      - uses: actions/checkout@v4
      - name: Build
        run: go build ./...
