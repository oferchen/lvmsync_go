# .hadolint.yaml
ignored:
  - DL3018  # allow unpinned apk/apt in CI; base image versioning controls drift
  - DL3013  # allow unpinned pip when managed via requirements.txt
  - DL3045  # allow omitting --no-install-recommends if base is slim
  - DL3059  # allow split RUN steps for caching
  - DL4001  # allow ADD where remote URLs or auto-extract are used
  - DL3025  # base image policy enforced elsewhere
  - DL4006  # default shell acceptable
trustedRegistries: []
