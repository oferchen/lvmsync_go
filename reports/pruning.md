Removed unused internal packages `device`, `frame`, and `hash`; updated module dependencies with `go mod tidy`.

Removed unreferenced packages `grpc/auth` and `internal/transport/{h2,quic,ssh,tcp}`; refreshed dependencies with `go mod tidy`.